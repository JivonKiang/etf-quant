# etf-quant 项目交接说明

> 生成时间：2026-08-13 ｜ 从 E 盘工作区迁移至 D:\Stocks and Mutual Funds\etf-quant

## 一、项目是什么

支付宝 ETF 联接基金（C 类，场外）量化策略，全自动信号监控 + 邮件提醒 + GitHub Pages 可视化 + 持仓跟踪。

## 二、策略（已定稿，不再改）

- **买入**：MA10 上穿 MA30（金叉）+ MACD 柱状图 > 0
- **持有**：20 个自然日，或涨幅达 +15% 提前止盈卖出
- **标的池**：8 只支付宝 C 类联接（中证500/创业板/上证50/纳指/标普/红利/半导体/科创50）
- **回测指标**：胜率 66.0%、平均每笔 +1.64%、最大回撤 5.8%、夏普 0.90、卡玛 1.38、历年 56%~80% 全 >50%

## 三、关键配置

| 项 | 值 |
|----|----|
| GitHub 仓库 | `JivonKiang/etf-quant`（public） |
| Pages 站点 | https://jivonkiang.github.io/etf-quant/ |
| 数据源 | 天天基金净值（T+1 滞后）+ 腾讯实时行情（场内 ETF 代理当日走势） |
| 邮件 | 163 SMTP（MAIL_* 5 个 secrets 已配在仓库） |
| 定时 | GitHub Actions 每天 14:00（更新数据）+ 14:45（发建议邮件） |
| 收件邮箱 | fmmujf@163.com |

## 四、文件结构

```
etf-quant/
├── config.py           # 标的池 + 策略参数 + ETF_MAP(场外→场内映射)
├── signal_monitor.py   # 信号监控 + 每日建议邮件（含实时涨跌）
├── build_site.py       # 生成前端 index.html
├── position_alert.py   # 持仓买卖提醒（止盈/止损/破位/加仓）
├── optimize.py         # 参数扫描引擎
├── etf_backtest.py     # 回测引擎
├── positions.json      # 用户实际持仓（手动回报录入）
├── signals.json        # 每日信号（自动生成）
├── .github/workflows/signal-check.yml  # 定时任务 + Pages 部署
└── nav_cache/          # 净值缓存
```

## 五、当前持仓（positions.json，4 只）

| 代码 | 名称 | 买入日 | 金额 |
|------|------|--------|------|
| 024418 | 华夏半导体材料设备ETF联接C | 08-11 | 28000 |
| 017997 | 万家沪深300成长ETF联接C | 08-06 | 20000 |
| 007531 | 华宝券商ETF联接C | 07-22 | 20000 |
| 020640 | 广发半导体设备ETF联接C | 08-13 | 5000 |

## 六、本地运行

```bash
python signal_monitor.py        # 检测信号 + 发日报
python signal_monitor.py --no-mail  # 不发邮件（CI 14:00 档）
python build_site.py            # 生成前端
python position_alert.py        # 持仓买卖提醒
python optimize.py              # 参数扫描
```

## 七、重要经验（务必记住）

1. **数据滞后**：场外净值 T+1，实时走势用「场内 ETF 实时价」代理（腾讯 qt.gtimg.cn 接口，config.ETF_MAP 映射）。
2. **网络**：github.com git push 被墙，但 api.github.com 可访问 → 推代码用 Contents API，建分支用 Git Data API，设 secrets 用 `gh secret set`。
3. **用户诉求**：只要胜率+盈利双高、减少人工负担，不管策略类型；数据必须实时/透明（标注日期），不糊弄。
4. **持仓同步**：支付宝无 API，只能手动回报（用户对话说「买入/卖出 代码 金额」）。
5. **用户偏好**：金融场景涨红跌绿（中国习惯）；文件命名用时间戳；不要"工作版/formatted"等乱命名。
6. **token**：写权限 GitHub token 用户需自行撤销，勿长期保留。
